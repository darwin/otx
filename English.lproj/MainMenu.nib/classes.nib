{
    IBClasses = (
        {
            ACTIONS = {
                openExe = id; 
                processFile = id; 
                selectArch = id; 
                showAboutBox = id; 
                showMainWindow = id; 
                showPrefs = id; 
                switchPrefsViews = id; 
                syncOutputText = id; 
                thinFile = id; 
            }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                mAboutPanel = NSPanel; 
                mArchPopup = NSPopUpButton; 
                mMainWindow = NSWindow; 
                mOutputText = NSTextField; 
                mPathText = NSTextField; 
                mPrefsOutFileBaseText = NSTextField; 
                mPrefsOutFileExtText = NSTextField; 
                mPrefsOutputView = NSView; 
                mPrefsProcessView = NSView; 
                mPrefsViewPicker = NSSegmentedControl; 
                mPrefsWindow = NSWindow; 
                mProgBar = NSProgressIndicator; 
                mProgDrawer = NSDrawer; 
                mProgText = NSTextField; 
                mSaveButton = NSButton; 
                mSaveMenuItem = NSMenuItem; 
                mThinButton = NSButton; 
                mTypeText = NSTextField; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = CDropBox; 
            LANGUAGE = ObjC; 
            OUTLETS = {mController = AppController; }; 
            SUPERCLASS = NSBox; 
        }, 
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = NSSegmentedControl; LANGUAGE = ObjC; SUPERCLASS = NSControl; }, 
        {
            ACTIONS = {switchViews = id; }; 
            CLASS = PrefsWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                mOutputView = NSView; 
                mProcessView = NSView; 
                mViewSelector = NSSegmentedControl; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}